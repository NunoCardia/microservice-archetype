spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/ms${modulePrefixUpperCamel}DB
  datasource:
    password: ''
    url: jdbc:h2:mem:testdb
    username: sa
  h2:
    console:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
